import { Fira_Code, Josefin_Sans } from 'next/font/google';

export const firaCode = Fira_Code({ display: 'swap', subsets: ['latin'] });

export const josefinSans = Josefin_Sans({
  display: 'swap',
  subsets: ['latin'],
  weight: ['300', '400'],
});
